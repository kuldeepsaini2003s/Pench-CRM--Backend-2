<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Invoice Preview</title>
    <style>
      * {
        box-sizing: border-box;
      }
      body {
        margin: 0;
        font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI",
          Roboto, "Helvetica Neue", Arial;
        background: #ffffff;
        color: #000000;
        padding: 12px;
        min-height: 100vh;
        -webkit-font-smoothing: antialiased;
      }

      .container {
        max-width: 1100px;
        margin: 0 auto;
      }

      /* header */
      .header {
        border-bottom: 1px solid #000000;
        padding-bottom: 8px;
        margin-bottom: 18px;
      }
      .logo {
        margin-top: 8px;
        margin-bottom: 8px;
      }
      .logo img {
        width: 112px;
        height: auto;
        object-fit: contain;
      }

      /* invoice title */
      .invoice-title {
        text-align: center;
        margin-bottom: 12px;
      }
      .invoice-title h2 {
        margin: 0;
        font-size: 34px;
        color: #1f8a4f;
        font-weight: 800;
      }

      /* top info */
      .top-info {
        display: table;
        width: 100%;
        font-size: 14px;
        margin-bottom: 14px;
        border-collapse: separate;
        border-spacing: 0;
      }
      .top-info .left {
        display: table-cell;
        width: 50%;
        vertical-align: top;
      }
      .top-info .right {
        display: table-cell;
        width: 50%;
        text-align: right;
        color: #6b7280;
        vertical-align: top;
      }

      /* bill grid */
      .bill-grid {
        display: table;
        width: 100%;
        margin-bottom: 12px;
        color: #6b7280;
        font-size: 14px;
        border-collapse: separate;
        border-spacing: 0;
      }
      .bill-grid > div {
        display: table-cell;
        width: 33.33%;
        vertical-align: top;
        padding-right: 20px;
      }
      .bill-grid > div:last-child {
        padding-right: 0;
      }
      .bill-grid h3 {
        margin: 0 0 6px 0;
        font-size: 16px;
        color: #111827;
        font-weight: 600;
      }

      /* table */
      .table-wrap {
        overflow-x: auto;
        margin-bottom: 16px;
      }
      table {
        width: 100%;
        border-collapse: collapse;
        font-size: 14px;
        table-layout: auto;
      }
      thead tr {
        background: #f3f4f6;
        font-weight: 700;
        text-align: center;
      }
      th,
      td {
        padding: 10px 12px;
        border-bottom: 1px solid #e5e7eb;
        text-align: center;
        word-wrap: break-word;
        overflow-wrap: break-word;
      }
      th:first-child,
      td:first-child {
        text-align: left;
        width: 30%;
      }
      th:nth-child(2),
      td:nth-child(2) {
        width: 20%;
      }
      th:nth-child(3),
      td:nth-child(3) {
        width: 15%;
      }
      th:nth-child(4),
      td:nth-child(4) {
        width: 20%;
      }
      th:last-child,
      td:last-child {
        width: 15%;
      }
      tbody tr {
        background: #fff;
        color: #111827;
      }

      /* footer totals */
      .footer {
        border-top: 1px solid #e5e7eb;
        padding-top: 12px;
        margin-top: 8px;
        font-size: 14px;
        color: #000000;
      }
      .totals {
        display: block;
        margin-bottom: 8px;
      }
      .totals .row {
        display: table;
        width: 100%;
        margin: 6px 0;
        color: #111827;
        border-collapse: separate;
        border-spacing: 0;
      }
      .totals .row > div:first-child {
        display: table-cell;
        text-align: left;
        width: 70%;
      }
      .totals .row > div:last-child {
        display: table-cell;
        text-align: right;
        width: 30%;
      }
      .status {
        margin-top: 6px;
        color: #111827;
      }
      .status-paid {
        color: #059669;
      }
      .status-partially-paid {
        color: #d97706;
      }
      .status-unpaid {
        color: #dc2626;
      }

      /* note */
      label {
        display: block;
        font-weight: 600;
        margin-bottom: 6px;
        color: #111827;
      }
      textarea {
        width: 100%;
        padding: 10px 12px;
        border: 1px solid #000000;
        border-radius: 6px;
        resize: vertical;
        font-family: inherit;
        font-size: 14px;
        color: #000000;
      }

      /* buttons */
      .actions {
        display: flex;
        justify-content: space-between;
        gap: 12px;
        margin-top: 14px;
        flex-wrap: wrap;
      }
      .btn {
        min-width: 130px;
        padding: 10px 14px;
        background: #374151;
        color: #fff;
        border-radius: 8px;
        border: 0;
        cursor: pointer;
        font-size: 14px;
        text-align: center;
      }

      /* small screens */
      @media (max-width: 420px) {
        .invoice-title h2 {
          font-size: 28px;
        }
        th,
        td {
          padding: 8px;
          font-size: 13px;
        }
      }
      .actions {
        display: flex;
        justify-content: space-between;
        gap: 12px;
        margin-top: 14px;
        flex-wrap: wrap;
        border-top: 1px solid #000000; /* 👈 added top border */
        padding-top: 12px; /* 👈 spacing so buttons don't stick to border */
      }
    </style>
  </head>
  <body>
    <div class="container">
      <!-- Header -->
      <header class="header">
        <div class="logo">
          <!-- replace src with your image path or keep as placeholder -->
          <img src="<%= logoBase64 %>" alt="Logo" />
        </div>
      </header>

      <!-- Invoice Title -->
      <section class="invoice-title">
        <h2>Invoice</h2>
      </section>

      <!-- Top Info -->
      <section class="top-info">
        <div class="left">
          <p style="margin: 0">INV- <%= invoice.invoiceNumber || 'N/A' %></p>
          <p style="margin: 4px 0 0 0">
            GST-No: <%= invoice.gstNumber || 'N/A' %>
          </p>
        </div>

        <div class="right" aria-label="dates">
          <p style="margin: 0; color:#000000;">
            <strong style="color: #000000; font-weight: 600"
              >Issue Date:</strong
            >
            <%= formattedDate || 'N/A' %>
          </p>
        </div>
      </section>

      <!-- Bill From / To / Duration -->
      <section class="bill-grid">
        <div>
          <h3>Bill from:</h3>
          <p style="margin: 0; line-height: 1.5; color: #000000">
            17, S Ambazari Rd, Madhav Nagar,<br />
            Nagpur 440010,<br />
            Maharashtra
          </p>
        </div>

        <div style="text-align: center">
          <h3>Bill to:</h3>
          <p style="margin: 0; line-height: 1.5; color: #111827">
            <%= invoice.customer?.name || 'N/A' %>
          </p>
        </div>

        <div style="text-align: right">
          <h3>Duration:</h3>
          <p style="margin: 0; line-height: 1.5; color: #111827">
            <%= periodStart || 'N/A' %> to <%= periodEnd || 'N/A' %>
          </p>
        </div>
      </section>

      <!-- Items Table -->
      <div class="table-wrap">
        <table aria-labelledby="items">
          <thead>
            <tr>
              <th>Item</th>
              <th>Product Size</th>
              <th>Quantity</th>
              <th>Price(₹)</th>
              <th>Total(₹)</th>
            </tr>
          </thead>
          <tbody>
            <% if (invoice.products && invoice.products.length > 0) { %> <%
            invoice.products.forEach(function(product) { %>
            <tr>
              <td><%= product.productName %></td>
              <td><%= product.productSize %></td>
              <td><%= product.quantity %></td>
              <td>₹<%= product.price %></td>
              <td>₹<%= product.totalPrice %></td>
            </tr>
            <% }); %> <% } else { %>
            <tr>
              <td colspan="5" style="text-align: center; color: #6b7280">
                No products found
              </td>
            </tr>
            <% } %>
          </tbody>
        </table>
      </div>

      <!-- Footer / Totals -->
      <section class="footer">
        <div class="totals">
          <div
            class="row"
            style="
              border-top: 1px solid #e5e7eb;
              padding-top: 8px;
              margin-top: 8px;
            "
          >
            <div style="font-weight: 700; color: #111827; font-size: 16px">
              Total Amount
            </div>
            <div style="font-weight: 800; font-size: 16px">
              ₹<%= (invoice.totals?.subtotal || 0) +
              (invoice.totals?.carryForwardAmount || 0) %>
            </div>
          </div>

          <div class="row">
            <div style="font-weight: 600; color: #414141">Paid Amount</div>
            <div style="font-weight: 700">
              ₹<%= invoice.totals?.paidAmount || 0 %>
            </div>
          </div>

          <div
            class="row"
            style="
              border-top: 1px solid #e5e7eb;
              padding-top: 8px;
              margin-top: 8px;
            "
          >
            <div style="font-weight: 700; color: #111827">Balance Amount</div>
            <% if ((invoice.totals?.balanceAmount || 0) > 0) { %>
            <div style="font-weight: 800; color: #dc2626">
              ₹<%= invoice.totals?.balanceAmount || 0 %>
            </div>
            <% } else { %>
            <div style="font-weight: 800; color: #059669">
              ₹<%= invoice.totals?.balanceAmount || 0 %>
            </div>
            <% } %>
          </div>
        </div>

        <div class="status">
          <p style="margin: 0">
            Payment Status:
            <span
              class="status-<%= (invoice.payment?.status || 'unpaid').toLowerCase().replace(' ', '-') %>"
              style="font-weight: 700"
              ><%= invoice.payment?.status || 'Unpaid' %></span
            >
          </p>
          <p style="margin: 6px 0 0 0">
            Payment Method: <%= invoice.payment?.method || 'COD' %>
          </p>
          <% if (invoice.totals?.paidDate) { %>
          <p style="margin: 6px 0 0 0">
            Paid Date: <%= new
            Date(invoice.totals.paidDate).toLocaleDateString('en-GB') %>
          </p>
          <% } %>
        </div>
      </section>
    </div>
  </body>
</html>
